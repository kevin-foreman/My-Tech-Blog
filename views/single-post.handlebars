<article class="post">
    <div class="title">
        <a>{{post.title}}</a>
    </div>
    <div>
        <a>{{post.post_content}}</a>
    </div>
    <div class="meta">
        by {{post.user.username}} on
        {{post.created_at}}
    </div>
</article>